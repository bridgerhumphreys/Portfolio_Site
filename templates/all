<!DOCTYPE html>
<html>
<head>
    <title>My Portfolio</title>
    <style>
        /* Your existing CSS styles here */
 #rippleCanvas {
            position: absolute;
            z-index: -10;
            border: 0px solid red;
            background-color: white;
            pointer-events: all; /* Allow clicks to pass through */
        }
        body {
        font-family: Arial, sans-serif;
        margin: 0; /* Remove default margin */
        padding: 0; /* Remove default padding */
        pointer-events: all; /* Allow clicks to pass through */
        }

        .navbar {
            position: fixed;
            top: 0;
            right: 0;
            background-color: #333;
            padding: 10px;
            pointer-events: all; /* Allow clicks to pass through */
        }
        .navbar button {
            background-color: #444; /* Button background color */
            color: white;
            border: none;
            padding: 10px 20px;
            margin: 4px 2px;
            cursor: pointer;
            border-radius: 4px; /* Optional: for rounded corners */
            font-size: 16px;
            transition: background-color 0.3s;
            position: relative; /* Important for the ripple effect */
            overflow: hidden; /* Ensures ripple effect stays within the button */
            background: transparent;
        }
        .navbar button:hover {
            background-color: #555; /* Color when hovered */
        }
        .navbar button:focus {
            outline: none; /* Removes the default focus outline */
        }
        .ripple {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.7); /* Color of the ripple */
            transform: scale(0);
            animation: ripple-effect 5s ease-out;
            pointer-events: all;
        }
        @keyframes ripple-effect {
            to {
                transform: scale(4);
                opacity: 0;
            }
        }
        .section {
            padding: 50px;
            height: 250px; /* Adjust as needed */
            text-align: center;
            pointer-events: all; /* Allow clicks to pass through */
        }
        html {
            scroll-behavior: smooth;
            pointer-events: all; /* Allow clicks to pass through */
        }
        .skill-subsections-container {
        z-index: 3;
        display: flex;
        flex-wrap: wrap;
        justify-content: center; /* Centers the subsections */
        gap: 20px; /* Spacing between subsections */
        padding: 20px;
        margin-top: 0px;
        background: transparent;

        }
        .skill-subsection {
        background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent gray */
        border-radius: 20px; /* Slightly rounded corners */
        padding: 20px;
        margin: 10px;
        color: white;

        /* Customize the size as needed, or use flex properties */
        flex: 1; /* Each subsection takes equal space */
        min-width: 300px; /* Minimum width */
        max-width: 300px; /* Maximum width */
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3); /* Optional: Adds a subtle shadow */
        }
        .skill-subsection button {
        opacity: 0;

        }



        @keyframes pulse-effect {
            0%, 20%, 40%, 60%, 80%, 100% {
                background-color: rgba(0, 0, 0, 0.5); /* Original color */
            }
            10%, 30%, 50%, 70%, 90% {
                background-color: rgba(0, 123, 255, 0.75); /* Pulsing color */
            }
        }

        .pulse {
            animation: pulse-effect 3s ease-in-out;
        }
        /* styles.css */
        .transparent-button {
            background-color: transparent;
            border: none;
            cursor: pointer;
            padding: 0;
            opacity: .9; /* Set the opacity to 90% */
        }

        .transparent-button img {
            width: 75px;
            height: 75px;
        }


    }


        /* Media query for adjusting the "Skills" section minimum height */
        @media screen and (min-height: 800px) {
            #skills {
                min-height: 800px; /* Adjust the minimum height as needed */
            }
        }
    </style>
</head>
<body>
    <canvas id="rippleCanvas" style="position: absolute; z-index: -5;"></canvas>

    <div class="navbar">
        <button data-target="#about">About</button>
        <button data-target="#projects">Projects</button>
        <button data-target="#skills">Skills</button>
        <button data-target="#contact">Contact</button>
    </div>

    <!-- About Section -->
    <div id="about" class="section">
        <h2>About Me</h2>
        <p>This is where you'll tell your story, your background, your skills, and your passions.</p>
    </div>

    <!-- Projects Section -->
    <div id="projects" class="section">
        <h2>Projects</h2>
        <p>Showcase your projects here. Describe each project, your role, technologies used, and the impact or results.</p>
    </div>

    <!-- Skills Section -->
    <div id="skills" class="section">
        <h2>Skills</h2>
        <!-- Skill buttons linking to subsections -->
        <button class="transparent-button" data-target="#labview-subsection">
            <img src="{{ url_for('static', filename='images/LabVIEW_icon.png') }}" alt="LabVIEW" style="height: 75px; width: 75px;">
        </button>
        <!-- Add other skill buttons here -->

        <!-- Skill Subsections -->
        <div class="skill-subsections-container">
            <!-- LabVIEW Skill Subsection -->
            <div id="labview-subsection" class="skill-subsection">
                <h3>LabVIEW</h3>
                <p>Details about your LabVIEW skills...</p>
            </div>
            <!-- Python Skill Subsection -->
            <div id="python-subsection" class="skill-subsection">
                <h3>Python</h3>
                <p>Details about your Python skills...</p>
            </div>
            <!-- Add other skill subsections here -->

            <!-- Contact Section (inside the Skills section) -->
            <div id="contact" class="skill-subsection">
                <h3>Contact</h3>
                <p>Provide details on how others can reach you, such as through email, social media, or a contact form.</p>
            </div>
        </div>
    </div>

    <!-- Contact Section (outside the Skills section) -->
    <!-- You can leave this section as it is outside of the Skills section -->
    <div id="contact" class="section">
        <h2>Contact</h2>
        <p>Provide details on how others can reach you, such as through email, social media, or a contact form.</p>
    </div>

    <script src="{{ url_for('static', filename='js/ripple.js') }}"></script>
    <script src="{{ url_for('static', filename='js/scrollToSection.js') }}"></script>
</body>
</html>
